{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "module": "ES2015",

    "sourceMap": true,
    // Declarations are generated by `tsconfig.json`. We don't need to generate
    // them again.
    "declaration": false,
    "outDir": "./dist/esm"
  }
}
